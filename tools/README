Manual native testing
---------------------

Content:
.
├── README
├── server-matching
│   ├── FullMatching_ignore_qparams.json
│   ├── FullMatching_passby_qparams.json
│   ├── FullMatching_sort_qparams.json
│   └── FullMatching_sortsemicolon_qparams.json
├── test.sh
└── tests
    ├── get_app_v1_foo_bar_1-name_test.no-filter-generalRandom
    :


README: this readme file
test.sh: manual test script helper which manages server matching configuration
         (stored at ./server-matching) as well as server provisioning and traffic
         tests.

         Each file within './tests' have three sections delimited with the
         tags: 'PROVISION', 'REQUEST_BODY' and 'REQUEST_HDRS'.
         Under them, the corresponding content will be used to provision
         the agent, and to know if the traffic request has a body and
         optional headers.
